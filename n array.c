#include<stdio.h>
void main()
{
   int a,b,c,n,i;
   printf("enter the values of n\n");   
   scanf("%d", &n);
   for(i=0;i<=n;i++)
   scanf("%d",&a[i]);
   for(i=0;i<=n;i++)
   {
       for(j=i;j<n;j++)
   }
   {
           if(a[i]>a[j])
               a[i]=a[j];
           }
   printf("%d",a[i]);
}
